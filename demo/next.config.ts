import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Nigerian Compliance AI — demo app built with RAG Kit.
  // Add rewrites, headers, or custom settings here as needed.
}

export default nextConfig
